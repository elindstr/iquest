// import {
//   UPDATE_PRODUCTS
// } from './actions';

export const reducer = (state, action) => {
  switch (action.type) {

    // example usage:
    // case UPDATE_PRODUCTS:
    //   return {
    //     ...state,
    //     products: [...action.products],
    //   };

    default:
      return state;
  }
};
